<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelversion>4.0.0</modelversion>

    <parent>
        <groupid>org.jenkins-ci.plugins</groupid>
        <artifactid>plugin</artifactid>
        <version>2.11</version>
        <relativepath />
    </parent>
    <groupid>com.ztbsuper</groupid>
    <artifactid>dingding-notifications</artifactid>
    <version>1.0</version>
    <packaging>hpi</packaging>

    <properties>
        <!-- baseline jenkins version you use to build the plugin. users must have this version or newer to run. -->
        <jenkins.version>1.625.3</jenkins.version>
        <!-- java level to use. java 7 required when using core >= 1.612 -->
        <java.level>7</java.level>
        <!-- jenkins test harness version you use to test the plugin. -->
        <!-- for jenkins version >= 1.580.1 use jth 2.x or higher. -->
        <jenkins-test-harness.version>2.13</jenkins-test-harness.version>
        <!-- other properties you may want to use:
             ~ hpi-plugin.version: the hpi maven plugin version used by the plugin..
             ~ stapler-plugin.version: the stapler maven plugin version required by the plugin.
        -->
    </properties>

    <name>dingding[钉钉] plugin</name>
    <description>钉钉jenkins通知插件</description>
    <url>https://wiki.jenkins-ci.org/display/jenkins/dingding+notification+plugin</url>

    <!-- the default licence for jenkins oss plugins is mit. substitute for the applicable one if needed. -->

    <licenses>
        <license>
            <name>mit license</name>
            <url>http://opensource.org/licenses/mit</url>
        </license>
    </licenses>

    <developers>
      <developer>
        <id>ztbsuper</id>
        <name>marvin zhang</name>
        <email>ztbsuper@gmail.com</email>
      </developer>
    </developers>
    <scm>
      <connection>scm:git:git://github.com/jenkinsci/dingding-notifications-plugin</connection>
      <developerConnection>scm:git:git@github.com:jenkinsci/dingding-notifications-plugin</developerConnection>
      <url>http://github.com/jenkinsci/dingding-notifications-plugin</url>
      <tag>dingding-notifications-1.0</tag>
  </scm>
    <repositories>
        <repository>
            <id>repo.jenkins-ci.org</id>
            <url>https://repo.jenkins-ci.org/public/</url>
        </repository>
    </repositories>
    <pluginRepositories>
        <pluginRepository>
            <id>repo.jenkins-ci.org</id>
            <url>https://repo.jenkins-ci.org/public/</url>
        </pluginRepository>
    </pluginRepositories>
    <dependencies>
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>fastjson</artifactId>
            <version>1.2.12</version>
        </dependency>
        <!--<dependency>-->
        <!--<groupId>org.jenkins-ci.plugins</groupId>-->
        <!--<artifactId>credentials</artifactId>-->
        <!--<version>1.9.4</version>-->
        <!--</dependency>-->

    </dependencies>

</project>
